/*using Draw;
using Entities;
using ShapeForm;
using System.Windows.Forms;

namespace Operations
{
  public class ShapeOperation : IShapeOperation
    {
        public void Draw(ERectangle rectangle)
        {
            Application.Run(new RectangleForm(rectangle));
        }

        public void Draw(Line shape)
        {
            Application.Run(new LineForms(shape));
        }

        public void Draw(Circle circle)
        {
            Application.Run(new CircleForms(circle));
        }
    }
}
*/